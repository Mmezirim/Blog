<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz Application</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h1 class="text-center">Quiz Application</h1>
        <form action="/submit" method="POST">
            <% for (let i = 0; i < quizQuestions.length; i++) { %>
                <div class="question">
                    <h2><%= quizQuestions[i].question %></h2>
                    <% for (let j = 0; j < quizQuestions[i].options.length; j++) { %>
                        <div class="form-check">
                            <input class="form-check-input" type="radio" name="option-<%= i %>" value="<%= j %>">
                            <label class="form-check-label"><%= quizQuestions[i].options[j] %></label>
                        </div>
                    <% } %>
                </div>
            <% } %>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
    </div>
</body>
</html>